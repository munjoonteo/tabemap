{
  "name": "tabemap-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "upload-data": "node upload.js https://tabemap-api.tabemap.workers.dev $TOKEN",
    "format": "prettier --write 'src/**/*.ts'"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "prettier": "^3.9.6",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
