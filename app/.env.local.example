# Copy this file to .env.local and fill in values after deploying the worker.
# Leave VITE_API_URL unset to use local IndexedDB instead (offline / dev mode).
# The write token is no longer stored here — it's entered at runtime via the 🔒 button.

VITE_API_URL=https://tabemap-api.YOUR_SUBDOMAIN.workers.dev
